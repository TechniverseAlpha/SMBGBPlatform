<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Quote Submitted</title>
  <link rel="stylesheet" href="/public/style.css">
</head>
<body>
  <header class="header">
    <img src="{{company.logoUrl}}" alt="{{company.name}} Logo" class="logo">
    <h1>Quote {{quoteId}} Submitted</h1>
  </header>

  <section class="summary">
    <h2>Quote Summary</h2>
    <ul>
      {{#each lines}}
        <li>{{name}} — Qty: {{qty}} — ${{price}} each</li>
      {{/each}}
    </ul>
  </section>

  <section class="info">
    <p><strong>Agent:</strong> {{agent.name}}</p>
    <p><strong>Customer:</strong> {{customer.name}}</p>
  </section>
</body>
</html>
